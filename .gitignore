# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Proptest auto-generated regression files are checked in by convention
# (so failing seeds reproduce on every machine), so we do NOT ignore them.

# Insta snapshot work-in-progress files (committed `.snap`, ignored `.snap.new`).
**/*.snap.new
.cargo-ok

# Node
node_modules/
dist/
.vite/
coverage/
frontend/src/wasm/*
!frontend/src/wasm/om_wasm.d.ts
!frontend/src/wasm/om_wasm.js

# Tauri build artifacts
crates/om-app/target/
crates/om-app/gen/schemas/

# Editor / OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
*.swp
om-app-windows-x86_64.zip
*.tsbuildinfo
